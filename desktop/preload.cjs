// Volontairement vide : la coque Electron n'expose aucune API native au
// bundle. Le fichier existe pour que webPreferences.preload ait une cible
// stable le jour où il faudra en exposer une (contextBridge).
